# Hanlin：论文已有Frozen Baseline复跑与结果回报

状态：2026-09-16最新分工，供用户下发。已有五组复跑继续，另增加PAFA任务，见[PAFA交接清单](2026-09-16_hanlin_pafa_handoff_zh.md)。PC-MCL/DCASE仍由项目侧承担。本轮没有直接发送给Hanlin。

Hanlin，这一轮请你重新运行论文当前已有的五组baseline：AST、BEATs、PANNs、OPERA-CT、HeAR。另由你负责PAFA，具体配置在单独的交接清单中统一；我们自己负责PC-MCL和DCASE。本文件只描述已有五组的复跑规范。

## 复跑范围

以当前Table1已有五行及项目提供的固定配置为准，使用已确认的5-s音频/frozen-encoder设置，每组形成三个真实下游训练seed的结果。请同时报告ICBHI Score、SPRSound official Score、HF CAS AUROC和KAUH patient BA。

各数据集究竟训练什么头、哪些标签用于监督、怎样选checkpoint、HF/KAUH属于哪种评测，要按原协议明确记录。如果旧代码、论文和配置不一致，请先指出，我们统一后再运行，不需要你自行补齐科学设计。不要使用旧2-s设置替代当前5-s设置，也不要仅为了统一输入而改掉encoder本身必要的前处理。

## 项目方需要提供

- 本次固定的五模型清单、版本/权重来源与可运行代码；
- 5-s准备、split/患者分组、任务标签与readout说明；
- 各训练任务的配置、三个seed及选模规则；
- 四列指标计算与结果输出格式。

先回复可用硬件和你目前持有的脚本/配置/数据版本，方便核对缺项。此处不要求提供账号或远程机器权限。

## 必须交回的结果

1. 每个encoder的权重来源、源监督历史、冻结和可训练模块。
2. 每个训练任务的seed、参数、数据划分、checkpoint/epoch和选择依据。
3. 逐样本ID、标签顺序/正类定义、概率或logits、最终预测，以及患者聚合规则。
4. 四项指标、必要的支持量和混淆矩阵，再汇总三seed mean ± sample SD。
5. 完整日志、实际用时，以及失败或与原表不一致的说明。

SPRSound官方Score不是简单BA；HF的CAS分数如何产生要明确；KAUH不能把同患者B/D/E当独立患者。若结果与旧表不同，请原样回报，不通过改正类方向、标签、seed或选模规则让数值接近旧表。

不只交最终均值表，不用已有单个checkpoint重复推理来代替三seed。不要新增无关模型、LSAA或Table2消融，不做hash/checksum、smoke/probe/profile。需要改变既定设置时，先回报原因，由我们统一决定。

项目内部计划9/22汇总结果、9/23完成投稿整理，不给你设个人硬截止。请说明实际可完成的范围；下一个工作周期前若尚未收到反馈，项目侧会自行完成仍缺的复跑。
